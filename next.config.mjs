// next.config.mjs
import path from 'path';

const serverRuntimeConfig = {
    PROJECT_ROOT: process.cwd(),
    // Add other server runtime configurations if needed
};

export {serverRuntimeConfig};

export default {
    serverRuntimeConfig,
    // Your other Next.js configuration options
};
